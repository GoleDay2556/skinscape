{
  "name": "skinscape",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "cd src/server && npm install && npm start",
    "dev": "concurrently \"cd src/server && npx nodemon\" \"cd src/client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": "^24",
    "npm": "^11"
  }
}
